apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: student-attendance

spec:
  selector:
    app: frontend

  type: ClusterIP

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP